# ── Certificates & Secrets ────────────────────
# Private key files — never commit
*.pfx
*.p12

# ── Local Configuration ──────────────────────
# Contains Hudu API key, tenant info, etc.
config.psd1
# Contains Hudu company slugs for batch runs
UnattendedCustomers.psd1

# ── Audit Output & Reports ───────────────────
*.html
*.pdf
*.log

# ── Resource Files (auto-downloaded) ─────────
*.csv

# ── Documents ────────────────────────────────
*.docx
*.dotx

# ── Data Files ───────────────────────────────
*.json
# version.json is tracked — override the blanket ignore
!version.json
# Azure Function scaffolding JSON is tracked
!AzureFunction/host.json
!AzureFunction/AuditBatchTimer/function.json

# ── Azure Function Local Settings ────────────
# Contains connection strings and secrets
AzureFunction/local.settings.json

# ── Scripts & Scratch ────────────────────────
*.py
Test-*
